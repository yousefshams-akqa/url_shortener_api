const routes = {
    base: "/api/urls",
    home: "/",
    shorten: "/shorten",
    code: "/:code",
    clicks: "/stats/:code",
    register: "/register",
    login: "/login",
    health: "/health"
}

module.exports = routes